{
  "name": "xena-voice-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "start": "npx serve dist -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1",
    "vite": "^5.0.0"
  }
}
