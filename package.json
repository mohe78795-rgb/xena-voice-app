{
  "name": "xena-voice-app",
  "version": "1.0.0",
  "description": "Voice chat web application",
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "npx serve dist -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
